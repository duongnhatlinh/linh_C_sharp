@model hethongquanlyluutrufiles.Models.File
@{
    ViewData["Title"] = "Chi tiết file";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="page-header">
    <h2 class="header-title">Chi tiết file</h2>
    <div class="header-sub-title">
        <nav class="breadcrumb breadcrumb-dash">
            <a asp-area="" asp-controller="Home" asp-action="TrangChuUser" class="breadcrumb-item"><i class="anticon anticon-home m-r-5"></i>Home</a>
            <a asp-area="" asp-controller="FilesReceived" asp-action="Index" class="breadcrumb-item">Danh sách file</a>
        </nav>
    </div>
</div>

<div class="card">
    <div class="card-body">
        <h4 class="card-title">Chi tiết</h4>
        <div class="table-responsive">
            <table class="product-info-table m-t-20">
                <tbody>
                    <tr>
                        <td>ID:</td>
                        <td class="text-dark font-weight-semibold">@Model.FileId</td>
                    </tr>
                    <tr>
                        <td>Tên file:</td>
                        <td>@Model.FileName</td>
                    </tr>
                    <tr>
                        <td>Loại file:</td>
                        <td>@Model.TypeFile.TypeFileName</td>
                    </tr>
                    <tr>
                        <td>Kích thước file:</td>
                        <td>@Model.FileSize<span> Kb</span> </td>
                    </tr>
                    <tr>
                        <td>Người cung cấp:</td>
                        <td>@Model.User.Username</td>
                    </tr>
                    <tr>
                        <td>Thời gian tạo:</td>
                        <td>@Model.DateUploaded</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>
<div>
    <a class="btn btn-primary" asp-area="" asp-controller="FilesReceived" asp-action="Index">Trở lại</a>
</div>

